// Utilizando o array criado no exercício anterior imprima
// o resultado da divisão de cada um dos elementos por 2 .

let lista=[];

for (let count=0; count<25; count+=1){
    lista[count]=(count+1);
}
for (let count=0; count<25; count+=1){
    console.log(lista[count]/2);
}
