// let names = ['João', 'Maria', 'Antônio', 'Margarida'];
// for (nomes of names){
//     console.log(nomes);
// }

// podemos alterar e exibir dados dentro do For Of
// sem perder os dados do array original

let numeros = [1,2,3,4,5];
for(let numero of numeros) {
  console.log(numero+100);
}

console.log(numeros);